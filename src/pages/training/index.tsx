import { ViewWrapper } from "@/styles/components";
import Logo from "@/components/Logo";

const Index = () => {
  return (
    <ViewWrapper>
      <Logo />
    </ViewWrapper>
  );
};

export default Index;
